apiVersion: v1
kind: Service
metadata:
  name: {{ include "repositorydb.name" . }}-svc1
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "repositorydb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.repositorydb.service1.type }}
  ports:
    - port: {{ .Values.repositorydb.service1.port }}
      targetPort: {{ .Values.repositorydb.service1.targetPort }}
      protocol: {{ .Values.repositorydb.service1.protocol }}
      {{- if and (or (eq .Values.repositorydb.service1.type "NodePort") (eq .Values.repositorydb.service1.type "LoadBalancer")) (not (empty .Values.repositorydb.service1.nodePort)) }}
      nodePort: {{ .Values.repositorydb.service1.nodePort }}
      {{ end }}
  selector:
    {{- include "repositorydb.selectorLabels" . | nindent 4 }}

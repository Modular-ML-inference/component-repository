version: '3.8'

services:
    flrepository:
        restart: always
        container_name: flrepository
        build: application
        networks:
            - default
        ports:
            - "9013:9012"
        depends_on:
            - repositorydb
    repositorydb:
        container_name: repositorydb
        restart: always
        build: mongo_db
        networks:
            - default

networks:
    default:
        driver: bridge
        name: custom_fl
